{
  "name": "@-amazeelabs/token-auth-middleware-test",
  "private": true,
  "description": "E2E tests for @amazeelabs/token-auth-middleware",
  "scripts": {
    "test:static": "tsc --noEmit && eslint . --quiet",
    "test:integration": "playwright install chromium && playwright test"
  },
  "devDependencies": {
    "@internal/eslint-config": "workspace:*",
    "@playwright/test": "1.50.0",
    "@types/node": "22.10.10",
    "typescript": "5.7.3"
  }
}
